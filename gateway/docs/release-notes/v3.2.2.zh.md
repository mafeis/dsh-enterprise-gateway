# v3.2.2 · Windows 装机全链路修复（内嵌 node 跑 pnpm、GBK 解码、store 自愈、启动存活验证）+ 接入页 UE 升级与局域网下载提速

> 上一版本：`v3.2.1`。本文件由 `npm run release` 生成，摘要可再编辑；
> 需要写进 GitHub Release 正文时直接整段粘过去。

## 本版本包含的提交（16 条）

- c33cde2 chore: 加 scripts/release.mjs 统一发布入口 — 撞号与同号漂移防复发
- e902f1d fix: pnpm 输出强制 UTF-8 解码，修 GBK 环境吞字母致自愈失效 — pnpm 错误码带 U+2009，UTF-8 字节被 PS5.1 按 GBK 解码把 ERR_PNPM_UNEXPECTED_STORE 变 鈥塃RR_...（E 被 89 45 双字节吞掉），正则匹配失效自愈未触发（真机乱码实录）；调用期 [Console]::OutputEncoding=UTF8 并还原，正则同时放宽为 RR_ 前缀双保险；pnpm 非零但插件实体在位时打注意放行（store 已与应用对齐的合法态）
- 256feef polish: 装机完成提示对齐真实体验 — 应用启动需加载企业插件（实测十几到半分钟），'已打开'改'正在启动+等待预期+找不到窗口怎么办'；win/mac 脚本尾语与 /setup 页第 4 步文案同步（中英）
- d0a2d22 fix: [4/6] 改验证式阶梯装插件 — 真机 runasnode 装完 rc=0 零输出且'未找到实体'（疑 Windows 包禁用 RunAsNode 熔丝，& app.exe 实为单实例移交静默退，或 GUI 子系统 stdout 不回显）；退出码不再可信：runasnode 冒烟须见版本号回显，每级试装以 node_modules 实体存在为唯一成功标准，逐级降级 sys pnpm 兜底，store 自愈保留
- f0d6bfa fix: [6/6] 启动带存活验证 + 已有进程去重 — diag 实测应用本体健康（15s 稳定/窗口正常/新启动零日志错误），'闪退'实为 WMI 拉起的实例未存活或脚本中断未到 [6/6]；WMI 启动后 4s 探测进程数，未存活回退 Start-Process，再无进程打印明确手动打开指引；检测到安装器已自启实例则跳过重复拉起
- a649978 chore: diag.ps1 v2 — 杀残留后启动并 15s 逐秒存活采样（RAM/窗口句柄/退出码/兄弟 DSH 进程），主日志+错误日志+harness home 日志三路尾部齐采，一次拍死应用秒退真因
- 33a026f fix: 自带 pnpm 改由应用内嵌 node 运行（RunAsNode），修 node>=22.13 与 EA=Stop 双炸点 — 真机：脚本用系统 node20 跑 pnpm11.8 触发 'requires at least Node.js v22.13' 警告，且脚本 EA=Stop 下 pnpm 任何 stderr 经 2>&1 变终止性错误掀掉脚本；应用同款 ELECTRON_RUN_AS_NODE=1 + app exe 实测内嵌 node 24.18.1/electron 43.3；三级回链 runasnode>node>=22.13>系统pnpm，调用块局部降 EA=Continue，win/mac 双端
- 091e541 fix: 装机脚本与桌面应用统一 pnpm（根因修复 store 互踩）— 应用启动迁移用自带 resources/app/node_modules/pnpm（11.8.0/store v11，实测），脚本用机器 pnpm 大版本不同即 ping-pong ERR_PNPM_UNEXPECTED_STORE；应用在场改用自带 pnpm.mjs + 应用同款默认 store（win 显式 --store-dir LOCALAPPDATA，真机证据应用不读用户 store-dir 配置），win/mac 双端，store 自愈保留兜底
- 2b7dd2d feat: /setup/diag.ps1 一键启动诊断收集器 — 前台跑应用带退出码、应用日志尾部、SecurityCenter2 杀软枚举（不依赖 Defender 模块）、崩溃转储、profile 选择/插件实体检查；纯 ASCII 防 PS5.1 粘贴乱码，供 IT 现场取证
- 64d0f16 fix: 装机脚本自愈 pnpm store 不一致 — 机器切换过 pnpm 大版本时 profile node_modules 记着旧 store，pnpm 拒装 ERR_PNPM_UNEXPECTED_STORE 且不可恢复（真机：pnpm12 装的 v11 store vs 脚本 pnpm10 + 机器自定义 store-dir）；[4/6] 捕获该错误按官方指引清 node_modules 重装，win/mac 双端，add_plugin 逻辑已假 pnpm 功能级实测
- 079c3c6 fix: windows-setup [6/6] 改用 WMI 脱离进程树启动 — Windows Terminal/新式 conhost 关窗口清扫整个进程树，Start-Process 挂在装机 PowerShell 树下的 DSH Desktop 被连带关闭（真机复现：关 PS 窗口即退出）；Win32_Process.Create 父进程为系统服务、不进控制台作业对象，失败回退 Start-Process
- eae7884 fix: windows-setup 预置自愈悬空 profile 选择 — 旧脚本残留 active=enterprise 指向已不存在的 profile，桌面端启动自愈只兜底 desktop 缺失/零 profile 两种形态，悬空名直接掉恢复模式（真机：profile "enterprise" does not exist）；预置时校验选择指向的 profile 目录真实性，坏指针纠正为 desktop
- 86b6954 perf: windows-setup 下载提速 — IWR 进度条是 PS 5.1 的性能黑洞（实测局域网 150MB 被拖到 0.4MB/s，网关回环实测 1.1GB/s 无责）；Get-RemoteFile 函数作用域关进度渲染，三处绕过统一下载口的直连 IWR 全部收编
- 4362156 feat: setup 接入页 UI/UE 升级 — 跟随系统深浅色；OS 分段切换取代 UA 硬隐藏（IT 替另一端引导可自由切换）；终端风代码块 + 内嵌复制按钮（复制成功打勾反馈）；步骤时间轴 + 连接竖线；版本下发状态胶囊（未镜像自动转灰点）；键盘焦点环/aria/reduced-motion 无障碍；中英双语与剪贴板降级逻辑不变
- fdfc5f4 fix: setup 接入页副标题去掉「4 步完成」— 3.2 起装机脚本是 [1/6]..[6/6] 六步流程，页面步数与脚本口径对不上；保留「全程约 5 分钟」预期，中英文同步改
- be46a70 docs: 订正 v3.2.1 说明 — 3.2.0 不是号位墓碑，是另一台机器的并发发布（gitHead 8a33d1d）
