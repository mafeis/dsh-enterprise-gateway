# DSH 企业网关 · 核心文档

> 版本 v3.0（2026-09-15）· 一切皆插件（Cordis 内核）· 零 npm 依赖（Node 22.5+ `node:sqlite`）
> 运行入口：`gateway/gateway.mjs` → `src/host.mjs`

---

## 1. 它是什么

企业 LLM 网关：员工端（DSH 终端）→ 企业网关（8899）→ 上游供应商。

- **模型名抽象**：员工只见企业模型 id（如 `deepseek`、`qwen`），真实上游映射在网关配置里，换上游员工无感
- **容灾**：模型级备用供应商（`fallbackProviders`），请求级熔断 60s，主家恢复自动回切
- **状态检测**：每供应商独立探测（间隔/连续失败阈值可配），连续失败自动停用、探测通过自动恢复
- **留痕审计**：SQLite 只追加 + 每日 Merkle 锚防篡改
- **DLP**：`sk-…` 密钥硬拦截；银行卡/手机号/身份证脱敏放行
- **思考档位**：统一 off/low/medium/high 四档，代理层双风格注入（GLM `thinking` + OpenAI `reasoning_effort`）
- **管理台**：壳 + 插件页面，供应商/模型/用户/计费/安全/审计/客户端管控/插件管理一体

## 2. 架构与文件地图

```
gateway/
├── gateway.mjs            # 启动入口（import src/host.mjs）
├── src/
│   ├── host.mjs           # Cordis 内核：插件装载/依赖注入/生命周期
│   ├── plugin-manifest.mjs# 插件清单解析（name/provides/inject/admin.nav）
│   ├── config.mjs         # 配置分层 + 供应商/模型 CRUD + 探针端点转发
│   ├── probe.mjs          # 探针实现（test/probe/probe-all/probe-levels/apply-probe）
│   ├── auth.mjs           # JWT(HS256 零依赖) + scrypt + 请求鉴权
│   ├── dlp.mjs            # 规则引擎：block(拦截) / mask(脱敏)
│   ├── store.mjs          # SQLite 留痕（只追加）+ 每日 Merkle 锚 + 用户/心跳
│   ├── upstream.mjs       # 上游转发：加权负载 + 失败容灾 + SSE 透传 + 思考参数注入
│   ├── core/              # http 工具 / router / static（@include + ?v= 版本化）
│   ├── routes/            # chat / responses / admin / plugin(心跳) / auth-routes / usage
│   └── plugins/           # ★ 11 个业务插件，一切皆插件
│       ├── ent-*.mjs      #   领域服务 + API（manifest 声明 provides/inject）
│       └── ent-*.web/     #   插件自带管理台页面（index.mjs 导出 {html,load,bind}）
├── admin-web/             # 管理台壳：登录 + 顶栏 + 侧边栏 + 路由（页面全部来自插件）
├── scripts/               # 运维脚本（业务体检/冒烟/密码重置/开发 JWT）
└── data/                  # 运行数据（gateway.db / .env / gateway-config.json，不入库）
```

**插件清单**（`/admin/plugins` 动态发现，禁用插件 = 页面与 API 一起消失）：
ent-router / ent-config / ent-registry / ent-meta / ent-store / ent-auth / ent-security / ent-upstream / ent-catalog / ent-users / ent-billing / ent-client / ent-audit / ent-console / ent-inspector / ent-notify / ent-design

**管理台页面结构**：壳（`admin-web/`）只管登录、顶栏、侧边栏与 hash 路由；每个插件 `.web/index.mjs` 声明 `admin.nav`（图标/标题/排序），router 按 `/admin/plugins` 清单动态注入导航并懒加载页面模块，页面 section id = `page-<路由id>`。

**启动**：

```powershell
# npm 全局安装（推荐）——任意目录启动，数据生成在启动目录 ./data
npm i -g dsh-enterprise-gateway
dsh-enterprise-gateway

# 从源码运行（开发者）
cd gateway
node gateway.mjs            # 前台
# 后台常驻（独立进程，勿用 Start-Job——job 回收会杀掉网关）：
Start-Process cmd '/c node gateway.mjs 1>>data\gateway-stdout.log 2>>data\gateway-stderr.log' -WindowStyle Hidden
```

**配置路径链**（重要）：`ENT_GATEWAY_CONFIG` → 不设则回落「**启动目录**/data/gateway-config.json」（源码在 gateway/ 里启动即 `gateway/data/`；npm 全局安装后在哪个目录启动就是哪个目录的 `./data`，`ENT_DATA_DIR` / `ENT_DB_PATH` 可整体搬走）。管理台改的是生效中的那个文件。

---

## 3. 核心机制

### 3.0 插件契约（一切皆插件）

**模块五件套**（内置与外部插件完全一致，见活样例 `examples/external-plugin/`）：

```js
export const name = 'my-plugin'          // 插件名（管理页展示/禁用粒度）
export const provides = ['my-service']   // 本插件提供的服务名（供其他插件 inject）
export const inject = ['router', 'store']// 依赖的服务名；装载时缺任一 → 本插件装载失败并明示原因
export const manifest = {                // 可选：能力声明 + 可公开信息（协作视图可见）
  capabilities: ['my.greet'],
  exposes: { greeting: (cfg) => `hello ${cfg?.greeting ?? 'world'}` },
}
export const admin = {                   // 可选：自带管理台页面（无此导出 = 纯服务插件）
  nav: { id: 'my-page', title: '我的页面', icon: 'puzzle', order: 100 },
  entry: 'index.mjs',                    // 相对 my-plugin.web/ 目录；模块导出 { html, load, bind }
}

export function apply(ctx) {
  const router = ctx.get('router')       // 取依赖服务（inject 声明过的才拿得到）
  const cfg = ctx.fiber.config ?? {}     // gateway-config.json 里 plugins.<name>.config
  ctx.effect(() => router.exact('GET', '/hello', handler), 'my-route')  // 返回清理函数，卸载时执行
  ctx.provide('my-service', { ... })     // 向其他插件提供本插件服务（配合 provides）
}
```

**协作方式**：

| 途径 | 用法 |
| --- | --- |
| **服务注入** | A `provides: ['x']` → B `inject: ['x']` 后 `ctx.get('x')`。装载顺序必须满足依赖（内置清单顺序即依赖顺序；security 必须先于 upstream） |
| **领域事件** | inject `registry` 后 `registry.emit('dom.event', payload)` / `registry.on(type, fn)`（ent-notify 靠这个把领域事件外发 webhook） |
| **协作视图** | inject `inspector` 可查其他插件 manifest 能力与 exposes（`inspector.query('my-plugin')`） |
| **共享基础设施** | inject `store`（SQLite 留痕）、`auth`（JWT 鉴权）、`dlp`（内容检测）、`config`（配置读写热更） |

**装载规则**（host.mjs，装载失败绝不静默）：

1. 内置插件按清单顺序装载（基座 → 服务 → 业务域 → 协作）；`plugins.<name>.enabled=false` 跳过且不 import
2. 外部插件：`gateway-config.json` 的 `plugins.<name>.path` 指向实现模块（相对路径基于进程 cwd），`config` 字段即 `ctx.fiber.config`
3. `inject` 的服务缺失 → 该插件装载失败，原因写入注册表并在启动日志明示；其余插件继续启动
4. 每个插件在 `/admin/plugins` 有快照（provides/inject/manifest/admin/来源），禁用粒度 = 单插件

**新增插件三步**：写模块（五件套）→ 在 `src/plugin-manifest.mjs` 登记 title/summary/category → 在 `plugin-map.zh.md` 补一行。带页面再补 `admin` 声明与 `<name>.web/` 目录（页面契约见 `admin-plugin-pages.zh.md`）。

### 3.1 供应商与密钥

- Provider = `id + baseUrl + 密钥 + 权重/超时/启停`；模型挂在 Provider 下（`providerId + upstreamModel`），支持容灾 `fallbackProviders`
- 密钥解析优先级（`resolveProviderApiKey`）：**明文 `apiKey` 优先 → 环境变量 `apiKeyEnv` → null**；`apiKeyEnv` 指向 `data/.env` 中 `ENT_PROV_<名字>_KEY` 风格变量
- `/admin/config` 对密钥脱敏：只回 `hasKey`（布尔）和 `apiKeyDirect`（布尔），**不回密钥原文**

### 3.2 容灾（config.mjs + upstream.mjs）

- 模型 `fallbackProviders` 声明备用供应商链；主家请求失败 → 同请求内即切容灾（员工无感），失败供应商熔断 60s
- 熔断过期自动回主家；模型主家与容灾全部不可用 → 终端侧该模型自动下架
- 转发时上游模型名跟随目标供应商（同名跟随，跨供应商模型名不一致时按配置映射）

### 3.3 状态检测（ent-catalog + probe.mjs）

- 每供应商可配：探测间隔（默认 30s）、连续失败 N 次自动停用、探测通过自动恢复
- 探测 = `GET {baseUrl}/models`（只关心可达 + 密钥有效），统计内存计数器：累计/成功/失败/平均延迟/最近探测时间
- 管理台「状态」弹窗看统计；被管理员手动禁用（enabled=false）的供应商不探测
- 端点：`GET /admin/providers/:pid/probe-stats`

### 3.4 模型探测体系（probe.mjs）

| 端点 | 作用 | 耗时预期 |
| --- | --- | --- |
| `POST /admin/providers/:pid/test` | 连通测试 + 上游 /models 目录 | ~100ms |
| `POST /admin/providers/:pid/probe-all` | 拉目录 + 并发快速探针（off+high 两档），一次出齐可用性 | 3-30s |
| `POST /admin/providers/:pid/probe-levels` | 单模型逐档探测：off/low/medium/high 每档一条真实请求 | 5-40s |
| `POST /admin/providers/:pid/apply-probe` | 把探测结论应用到企业模型目录 | 即时 |
| `POST /admin/providers/:pid/probe` | 单模型单次探针（兼容旧入口） | ~1s |

**探针判定逻辑**（`probeProviderModelLevels`）：

- 每档发真实请求，记录 `accepted`（HTTP 200）/ `thought`（有思考痕迹）/ `ms`
- 思考痕迹字段**三兼容**：`reasoning_content`（DeepSeek/OpenAI 系）/ `reasoning`（GLM 系中转）/ `reasoning_text`
- 400 + 错误文含 reasoning/effort/thinking → 判"参数被拒"；404/503+model → "模型不存在"
- `supportedThinking` = 实测产生思考输出的档位；`supportsThinkingSwitch` = off 档无思考 且 有档位有思考
- 应用到模型目录：`thinkingLevels = ['off', ...实测思考档位]`；全无思考 → `thinking: 'none'` 仅 off
- 多模态实测：图片（真实图问答判定）/ 视频（上游 400 即判不支持），结论持久化在模型 `lastProbe.modalities`

**UI 流程**：供应商卡片「测试」→ 弹窗列上游全部模型（快速判定 + 筛选条）→ 每行「逐档测试」展开四档明细 → 「应用」写入企业模型目录。

### 3.5 思考参数注入（upstream.mjs `applyThinking`）

员工请求 `reasoning_effort: low|medium|high`（或缺省）→ 网关按模型配置转换后发上游：

- 档位取请求值 → 模型 `defaultThinking` → `'off'`
- `thinking: optional` + 非 off → 双发 `thinking:{type:'enabled'}` + `reasoning_effort:<level>`（GLM/Zhipu 风格 + OpenAI 风格兼容）
- `off` → `thinking:{type:'disabled'}`（仅当 `thinking !== 'none'`）

### 3.6 心跳契约（/heartbeat）

插件定时 POST `{ profile, env, policyVersion, device? }`；网关按 `sha256(Authorization 头)` 作为 device_hash 落库。
响应：`{ ok, deviceAccepted, modelFingerprint }`——`deviceAccepted=false` 表示网关无该设备快照；`modelFingerprint` = 企业模型 id 列表的 sha256 前 16 位，客户端据此检测模型变更。

### 3.7 管理台静态资源版本化（static.mjs）

- `composeIndex()`：展开 index.html 的 `@include` 注释，给 `/admin/static/...` 引用追加 `?v=<admin-web 全目录最大 mtime>`（文件一改即换 URL，且稳定不变 → 可被缓存）
- `versionMjs()`：模块间相对 import 追加 `?v=<文件mtime>`
- 缓存头：带 `?v=` 的资源 `cache-control: public, max-age=31536000, immutable`（浏览器+CDN 长缓存）；无版本参数兜底 `max-age=60`；index.html 本身仍 `no-store`
- **但**：各 .mjs 内部的 import 若**硬编码** `?v=<旧时间戳>`，mtime 变了也不会自动更新

---

## 4. 配置参考（gateway-config.json）

```jsonc
{
  "providers": [{
    "id": "provider-a",
    "name": "provider-a",
    "baseUrl": "https://gw-upstream.example.com/v1",
    "apiKey": "sk-…",              // 明文直存（优先生效）
    "apiKeyEnv": "ENT_PROV_EXAMPLE_KEY", // 备选：环境变量（data/.env）
    "timeoutMs": 120000,
    "weight": 10,
    "enabled": true,
    "probe": { "enabled": true, "intervalSec": 30, "failThreshold": 2 }
  }],
  "models": [{
    "id": "deepseek",              // 员工用的模型名
    "displayName": "DeepSeek V4 旗舰",
    "providerId": "provider-a",
    "upstreamModel": "deepseek-v4-flash",  // 真实上游名
    "fallbackProviders": ["provider-b"],   // 容灾链（按序）
    "thinking": "optional",        // optional | always | none
    "thinkingLevels": ["off","low","medium","high"],
    "defaultThinking": "off",
    "contextWindow": 128000,
    "maxTokens": 32768,
    "inputModes": ["text","image"], // text/image（video 由探针实测判定）
    "pricePer1MIn": 2, "pricePer1MOut": 8, "pricePer1MCacheIn": 0.04,
    "lastProbe": { /* 逐档/多模态探测结论持久化 */ },
    "enabled": true
  }]
}
```

校验红线（`validateModel`）：`thinkingLevels` 必须是数组且元素 ∈ off/low/medium/high；`defaultThinking` 必须在 `thinkingLevels` 内。
密钥环境变量：`data/.env` 一行一个 `ENT_PROV_<名字>_KEY=sk-xxx`（.env 与 gateway.db 均不入库）。

---

## 5. 端点速查

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/auth/login` | 登录换 JWT（`{token,user,policy}`） |
| POST | `/v1/chat/completions` | OpenAI 兼容，流式/非流式 |
| GET | `/v1/models` | 企业模型目录（公开） |
| GET | `/health` | 健康检查（版本/策略版本） |
| GET | `/admin/config` | 配置总览（密钥脱敏） |
| GET | `/admin/plugins` | 插件清单（含 admin.nav，管理台导航数据源） |
| GET/PATCH | `/admin/nav-config` | 侧边菜单自定义排序/显隐 |
| POST/PATCH/DELETE | `/admin/providers[...]` | 供应商 CRUD + 启停 |
| POST/PATCH/DELETE | `/admin/models[...]` | 模型 CRUD（PATCH 支持 lastProbe 持久化） |
| POST | `/admin/providers/:pid/probe-all` / `probe-levels` / `apply-probe` | 探测三件套 |
| GET | `/admin/providers/:pid/probe-stats` | 供应商探测统计（状态检测） |
| GET/POST/PATCH/DELETE | `/admin/users[...]` | 用户管理 |
| GET | `/admin/usage?days=14` | 计费视图 |
| GET | `/admin/logs?user=&limit=` | 留痕检索 |
| POST | `/heartbeat` | 插件心跳 |
| PATCH | `/admin/policy` | 策略热更 |

**排错入口**：`<启动目录>/data/gateway-stdout.log`（全部请求日志，含探测/编辑/登录记录）。
**运维脚本**：`node scripts/check-business.mjs`（心跳/最近请求/今日统计）· `node scripts/smoke.mjs`（冒烟）· `node scripts/reset-admin-pw.mjs <新密码>`（重置管理员密码）。
